apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: 
  name: helm-app
  namespace: argocd
spec: 
  destination: 
    namespace: helm-app
    server: "https://kubernetes.default.svc"
  project: default
  source: 
    path: helm-guestbook
    repoURL: "https://github.com/robinisme2/argocd-example-apps.git"
    targetRevision: master
    helm:
     releaseName: my-release2
  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    
